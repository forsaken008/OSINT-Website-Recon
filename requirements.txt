# Deep Web Recon — Python dependencies
# Install with: pip install -r requirements.txt
#
# tkinter is part of the Python standard library.
#   Windows/macOS: included with the standard Python installer.
#   Linux:         sudo apt install python3-tk   (Debian/Ubuntu)
#                  sudo dnf install python3-tkinter  (Fedora)

requests==2.31.0
dnspython==2.6.1
ipwhois==1.2.0
